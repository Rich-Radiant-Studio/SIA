# User Web Application

C端用户系统 - 家居装修平台的用户端应用

## 功能特性

- 🏠 **首页展示** - 软装案例展示，筛选功能，颜色选择器
- 🛍️ **商城功能** - 商品浏览和购买
- 👨‍🎨 **设计师** - 设计师展示和详情
- 🏘️ **社区** - 用户交流社区
- 🛒 **购物车** - 商品管理
- 👤 **个人中心** - 用户信息管理
- 🌍 **多语言支持** - 中文、英文、西班牙语

## 技术栈

- Vue 3 + TypeScript
- Vue Router 4
- Bootstrap 5 + Bootstrap Icons
- Vite
- 微前端架构 (Wujie)
- 第三方i18n插件

## 多语言功能

### 支持的语言
- 🇨🇳 中文 (zh)
- 🇺🇸 English (en)  
- 🇪🇸 Español (es)

### 语言切换
- 用户可以通过页面右上角的语言切换器选择语言
- 语言选择会自动保存到本地存储
- 支持实时切换，无需刷新页面

### 开发者使用方式

#### 1. 在组件中使用翻译
```vue
<template>
  <div>
    <h1>{{ $t('page.title') }}</h1>
    <p>{{ $t('page.description') }}</p>
  </div>
</template>

<script setup lang="ts">
import { inject } from 'vue'
import type { I18nPlugin } from '@/plugins/i18n'

const i18n = inject<I18nPlugin>('i18n')
const $t = (key: string) => i18n?.t(key) || key
</script>
```

#### 2. 添加新的翻译
在 `src/plugins/i18n.ts` 文件中的 `languages` 对象中添加新的翻译键值对：

```typescript
const languages = {
  zh: {
    translations: {
      'your.new.key': '你的中文翻译'
    }
  },
  en: {
    translations: {
      'your.new.key': 'Your English Translation'
    }
  },
  es: {
    translations: {
      'your.new.key': 'Tu Traducción en Español'
    }
  }
}
```

#### 3. 使用语言切换组件
```vue
<template>
  <LanguageSwitcher />
</template>

<script setup lang="ts">
import LanguageSwitcher from '@/components/LanguageSwitcher.vue'
</script>
```

## 开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build
```

## 端口配置

- 开发环境: http://localhost:8084
- 生产环境: 通过主应用 (main-web) 加载

## 项目结构

```
src/
├── components/          # 组件
│   └── LanguageSwitcher.vue # 语言切换器
├── plugins/            # 插件
│   └── i18n.ts         # 国际化插件
├── views/              # 页面组件
│   ├── Home.vue        # 首页 (主要页面)
│   ├── Shop.vue        # 商城
│   ├── Designers.vue   # 设计师列表
│   └── ...
├── router/             # 路由配置
├── App.vue             # 根组件
└── main.ts             # 入口文件
```

## 微前端集成

该应用作为微前端子应用运行在主应用 (main-web) 中，支持:
- 自动挂载/卸载
- 状态共享
- 跨应用通信
- 语言同步

## 主要页面

### 首页 (Home.vue)
- 完整的软装案例展示页面
- 左侧筛选器：风格、颜色、材质、档次、特色
- 颜色选择器，支持自定义颜色
- 案例卡片展示，支持点赞、收藏、分享
- 响应式设计，适配各种屏幕尺寸
- 完整的多语言支持

### 其他页面
- 所有页面都支持多语言切换
- 统一的语言切换器组件
- 简化的占位符组件，可根据需要逐步完善功能
